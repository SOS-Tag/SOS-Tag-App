mutation ConfirmUser($token: String) {
  confirmUser(token: $token) {
    response
    error {
      type
      code
      title
      message
      timestamp
      fields {
        type
        name
        detail
      }
    }
  }
}